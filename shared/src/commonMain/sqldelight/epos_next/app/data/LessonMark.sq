import epos_next.app.domain.entities.MarkUnitPeriods;
import kotlin.collections.List;

CREATE TABLE lessonMarkModel(
    lesson TEXT UNIQUE NOT NULL PRIMARY KEY,
    periods TEXT AS List<MarkUnitPeriods> NOT NULL,
    total INTEGER AS Int
);

insert:
INSERT OR IGNORE INTO lessonMarkModel(lesson, periods, total)
VALUES (?, ?, ?);

deleteAll:
DELETE FROM lessonMarkModel;

getAll:
SELECT * FROM lessonMarkModel;